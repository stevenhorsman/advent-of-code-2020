matrix:
  include:
    - language: python
      python:
        - "3.7"
      cache: pip
      install:
        - pip install -r requirements.txt
      script:
        - pytest
    - language: go
      go:
        - 1.x